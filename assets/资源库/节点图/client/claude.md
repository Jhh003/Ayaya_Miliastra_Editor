## 目录用途
- 存放客户端侧节点图脚本（`graph_type: client`）。
- 主要用于不依赖服务器运行时的本地逻辑表达、表现层逻辑与实验性验证。

## 文档约定（重要）
- 这里只描述目录用途与规范，不记录具体资源清单/不解释单个资源是做什么的
- 本 `claude.md` **只描述目录用途、通用约定与注意事项**。
- **不在此列举具体节点图文件/子目录清单**，也不解释单个节点图“是干什么的”。

## 当前状态
稳定可用。目录按用途分层组织（不在本文档维护清单）。

## 注意事项
- 节点图使用类结构 Python 文件保存，文件头部需包含基础元信息（`graph_id` / `graph_name` / `graph_type` / `folder_path` / `description`）以便索引与校验。
- 客户端节点图通常不直接参与服务器事件注册；一般由战斗系统、动画时间轴或客户端侧调度触发，图内可用于本地表现或向服务器转发请求。
- 本文件不记录修改历史，只描述本目录的用途、当前状态与使用注意事项。


