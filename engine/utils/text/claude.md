# text 子包

## 目录用途
集中存放文本相关的通用工具，目前主要用于字符串与中文文本的相似度/距离计算，可被自动命名、模糊匹配等功能复用。

## 关键文件
- `text_similarity.py`：文本相似度与距离计算工具（如编辑距离、近似匹配等）。

## 注意事项
- 不依赖应用层和 UI，仅使用标准库。
- 算法需兼顾准确性与性能，适合作为底层工具被多处调用。
- 不在此处引入 I/O 或日志副作用，保持纯函数风格为主。

---
注意：本文件不记录修改历史，仅描述“目录用途、当前状态、注意事项”。请在结构调整后保持描述同步。

