from __future__ import annotations
from typing import Any

# 本文件为类型桩（stub），仅供 Pyright 等静态检查器使用，运行时不生效。
# 运行时节点函数导出由 `runtime.engine.graph_prelude_client` 负责（基于 V2 AST 清单加载并注入）。

def 通知服务器节点图(*args: Any, **kwargs: Any) -> Any: ...


